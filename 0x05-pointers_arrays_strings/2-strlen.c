#include "main.h"
#include <string.h>
/**
 * _strlen -> A function that returns the length of a string
 * @s: string pointer to the function
 * Return: length of a string
 */
int _strlen(char *s);
{
	return (strlen(s));
}
